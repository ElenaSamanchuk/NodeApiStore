{
    "scripts" : {
        "homepage": "https://ElenaSamanchuk.github.io/NodeApiStore/",
        "main" : "app.js",
        "start": {
            "predeploy": "npm run build",
            "deploy": "gh-pages -d build",
            "node app.js --port=3000 --hostname=127.0.0.1"
        }
    }
}
